# Optional. Defaults to .cache/sleeper under the current working directory.
SLEEPER_CACHE_DIR=

# Optional diagnostics. Valid values: silent, error, info, debug.
SLEEPER_LOG_LEVEL=error

# Used only by `pnpm test:live`.
SLEEPER_LIVE_LEAGUE_ID=
SLEEPER_LIVE_USER=
